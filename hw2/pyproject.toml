[project]
name = "hw2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "onnxruntime",
    "tokenizers",
    "numpy",
    "tqdm",
    "minsearch",
    "gitsource",
]

[dependency-groups]
dev = [
    "huggingface-hub",
    "jupyter",
]
